#!/bin/bash


readonly PKGBUILDS=/home/fox/Git/PKGBUILDS
readonly AURDEST=/home/fox/.cache/aur

pkgname=$(basename `pwd`)

source /usr/share/makepkg/util.sh && colorize

for pkg in $(aurchain $pkgname); do
    if [[ ! $pkg == $pkgname ]]; then
        echo $pkg
    fi
done




